interface:
  display_name: "HelloSearch"
  short_description: "Real-web search, verification, and evidence synthesis"
  default_prompt: "Use $hellosearch to plan live web research, verify current facts, and answer with traceable sources."
policy:
  allow_implicit_invocation: true
